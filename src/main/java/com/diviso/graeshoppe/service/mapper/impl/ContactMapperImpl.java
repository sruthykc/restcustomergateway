package com.diviso.graeshoppe.service.mapper.impl;

/*import com.diviso.graeshoppe.client.customer.model.Contact;
import com.diviso.graeshoppe.client.customer.model.ContactDTO;
import com.diviso.graeshoppe.service.mapper.ContactMapper;

import java.util.ArrayList;
import java.util.List;
import javax.annotation.Generated;
import org.springframework.stereotype.Component;

@Generated(
    value = "org.mapstruct.ap.MappingProcessor",
    date = "2019-12-04T07:50:13+0530",
    comments = "version: 1.2.0.Final, compiler: javac, environment: Java 1.8.0_222 (Private Build)"
)
@Component*/
public class ContactMapperImpl  {

 /*  // @Override
    public Contact toEntity(ContactDTO dto) {
        if ( dto == null ) {
            return null;
        }

        Contact contact = new Contact();

        contact.setEmail( dto.getEmail() );
        contact.setId( dto.getId() );
        contact.setMobileNumber( dto.getMobileNumber() );
        contact.setPhoneCode( dto.getPhoneCode() );
        contact.setTelephone( dto.getTelephone() );

        return contact;
    }

   // @Override
    public ContactDTO toDto(Contact entity) {
        if ( entity == null ) {
            return null;
        }

        ContactDTO contactDTO = new ContactDTO();

        contactDTO.setEmail( entity.getEmail() );
        contactDTO.setId( entity.getId() );
        contactDTO.setMobileNumber( entity.getMobileNumber() );
        contactDTO.setPhoneCode( entity.getPhoneCode() );
        contactDTO.setTelephone( entity.getTelephone() );

        return contactDTO;
    }

   // @Override
    public List<Contact> toEntity(List<ContactDTO> dtoList) {
        if ( dtoList == null ) {
            return null;
        }

        List<Contact> list = new ArrayList<Contact>( dtoList.size() );
        for ( ContactDTO contactDTO : dtoList ) {
            list.add( toEntity( contactDTO ) );
        }

        return list;
    }

   // @Override
    public List<ContactDTO> toDto(List<Contact> entityList) {
        if ( entityList == null ) {
            return null;
        }

        List<ContactDTO> list = new ArrayList<ContactDTO>( entityList.size() );
        for ( Contact contact : entityList ) {
            list.add( toDto( contact ) );
        }

        return list;
    }*/
}
